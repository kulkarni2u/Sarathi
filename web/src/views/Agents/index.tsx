import { Placeholder } from "../../components/Placeholder";

/**
 * Agents — provider runtime, transport posture, and Sarathi roles.
 * Route: /agents
 *
 * Later workers: source data from GET /providers?workspace_id=... via
 * api.getProviders() in src/api/client.ts.
 */
export default function Agents() {
  return <Placeholder name="Agents" />;
}
