import { Placeholder } from "../../components/Placeholder";

/**
 * Settings — providers & routing, policy pack, governance, preferences.
 * Route: /settings
 *
 * Later workers: source data from GET /providers, GET
 * /workspaces/{id}/policy-pack, and related governance endpoints.
 */
export default function Settings() {
  return <Placeholder name="Settings" />;
}
